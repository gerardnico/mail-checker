
# The resolver, ie the DNS server (cloudflare)
resolver: 1.1.1.1

# The mail servers
mailers:
  - 188.245.43.250

# The domain of the mail servers
domains:
  - bytle.net
  - eraldy.com


pushgateway:
  # or env MAIL_CHECKER_PUSHGATEWAY_URL
  # without metrics
  # kubee kubectl port-forward -n monitoring svc/pushgateway 9091
  url: 'http://127.0.0.1:9091' # the url