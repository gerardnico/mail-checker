
# The resolver, ie the DNS server (cloudflare)
resolver: 1.1.1.1

# The mail servers
mailers:
  - 188.245.43.250

# The domain of the mail servers
domains:
  - bytle.net


pushgateway:
  # or env MAIL_CHECKER_PUSHGATEWAY_URL
  url: '' # the url